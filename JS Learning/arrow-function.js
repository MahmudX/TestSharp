let names = ['Mahmud', 'Habiba', 'Mamun', 'Siddika'];

/* let hi = () => {
    console.log('Hello!')
};
hi();

let printName = (name) => {
    console.log(`Hello ${name}`)
};
printName('Mahmud');
let add = (a, b) => {
    return a + b
};
console.log(add(5, 6)); */

/* names.map((name) => {
    console.log(`Hello ${name}`);
}); */

let i = 0;
names.map((name) => {
    i++;
    console.log(`${i}. Hello ${name}`);
});
